# network-intrusion-dashboard
Real-time intrusion detection and traffic monitoring system built with Python, Scapy, and Flask. Tracks network traffic, detects suspicious behavior (like SYN floods, DNS abuse, or HTTP leaks), and displays live alerts with device/blocking control via a web dashboard.
